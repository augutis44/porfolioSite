const tailwindClasses = {
  carousel: 'relative w-96 h-96',
  iconsContainer: 'absolute w-full h-full flex justify-center items-center',
  icon: 'absolute w-10 h-10 bg-blue-500 rounded-full cursor-pointer transition-transform duration-500 ease-in-out',
};

export default tailwindClasses;
